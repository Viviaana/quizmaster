import React from "react";

// Context setup to allow different pages to access the global state, allowing for login details to be passed between screens 


const AppContext = React.createContext();

export default AppContext;
